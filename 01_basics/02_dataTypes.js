"use strict"; /* treat all js code as a newer version of js. 
we cant change use strict once we have written it in an file
though we dont need it as engine will consider everything as new version but good to use these syntax*/

//alert(3+3) we are using nodejs. and not in web browser thats why it will give error;

let name = "pandey";
let age = 18;
let isLoggedIn = false;
// number =>2 to power 53;
// bigint
// string =>"";
// boolean => true/false;
// null => standalone value;
//  undefined=> abhi value assign nhi kia h
// symbol => umique k lia iska use hai

// object;
// NaN   not a number of type number

console.log(typeof 'shubham')
console.table([typeof undefined,typeof null]);